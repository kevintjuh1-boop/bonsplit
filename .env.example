# Copy this file to .env (never commit .env itself — it holds real secrets) and fill in real values.
SITE_AUTH_PASSWORD=choose-a-real-passphrase-here
RECEIPT_PARSING_PROVIDER=Development
ANTHROPIC_API_KEY=
